# Progress Tracker

Last updated: 2026-05-15
Current phase: PHASE_13_NOTIFICATIONS_AUDIT_SETTINGS
Status: PENDING

## Phases
- [x] PHASE_0_BOOTSTRAP — Scaffold, tracking files, types, utilities
- [x] PHASE_1_FOUNDATION — Design system, formatters, calculators, mock loader
- [x] PHASE_2_APP_SHELL — Role switcher, sidebar, topbar, command palette
- [x] PHASE_3_EXECUTIVE_DASHBOARD — KPIs, charts, AI feed, sustainability, network health
- [x] PHASE_4_OPERATIONS_DASHBOARD — Priority tiles, value-at-risk, top risky table, lanes, overdue banner, AI approve, heat-map, live pulse
- [x] PHASE_5_RISKY_PRODUCTS_LIST — Filters toolbar, TanStack table, grid, heatmap, preview drawer, approve / reject / bulk flows, CSV export, saved views
- [x] PHASE_6_PRODUCT_DETAILS — DEMO CENTERPIECE
- [x] PHASE_7_WHATIF_SIMULATOR
- [x] PHASE_8_RECOMMENDATIONS
- [x] PHASE_9_TASKS — manager + employee mobile views
- [x] PHASE_10_TRANSFERS_DISCOUNTS
- [x] PHASE_11_ANALYTICS
- [x] PHASE_12_DATA_QUALITY
- [ ] PHASE_13_NOTIFICATIONS_AUDIT_SETTINGS
- [ ] PHASE_14_POLISH_DEMO

## Notes
- 2026-05-15 — PHASE_0 done. Next.js 14 scaffold, shadcn slate base, all 34 UI components, 19 mock-data JSONs preserved, type system + formatters + risk/scenario calculators + mock-loader + permissions in place, every route has placeholder page.
- 2026-05-15 — PHASE_1 closed. PHASE_0 covered all foundation deliverables (types, constants, utils, formatters, risk/scenario calcs, mock-loader, permissions, globals.css risk vars, all 15 route placeholders). `tsc --noEmit` clean. No gaps to fill.
- 2026-05-15 — PHASE_2 done. 5 Zustand stores (auth/ui/filters/notifications/actions) with persist where applicable. Layout shell rebuilt: `(app)/layout.tsx` mounts AppHydrator + RouteGuard + CommandPalette + DesktopSidebar + Topbar. Sidebar collapsible (persisted) with mobile Sheet variant, nav groups filtered by role, mini user card. Topbar pieces: Breadcrumbs (resolves product IDs to names), DemoBadge (pulsing), Search trigger, DateRangePill, NotificationsPopover (unread badge + tabs + mark-all-read), ThemeToggle (sun/moon/system), RoleSwitcher (grouped popover with search), Avatar dropdown. CommandPalette via cmdk with Pages / Top Risky Products / My Tasks / Quick Actions. Hotkeys ⌘K (palette) and ⌘⇧R (role switcher) via `useKeyboardShortcut`. RouteGuard redirects on disallowed routes with sonner warning. `tsc --noEmit` and `next build` both clean (19 routes).
- 2026-05-15 — PHASE_3 done. Executive dashboard wired to filters-store date range with previous-period comparisons. 5 badges (risk/status/action/priority/confidence). KpiCard with tone variants, optional sparkline, change indicator, info tooltip, skeleton. 6 KPI grid (potential, actual, net saved, AI acceptance, task completion, waste). Loss-trend ComposedChart with Daily/Weekly toggle. Sustainability card (kg saved, ≈ CO₂ avoided, parcel count, mock ESG export). Top risky stores horizontal BarChart → /operations?store=…. Top risky categories donut + legend → /products?category=…. Net-saved waterfall (potential → −discount → −transfer → −operational → net saved). Latest AI Recommendations + Critical Tasks Today feeds. Network Health Banner (avg confidence, open issues, active stores, last sync). Installed `react-is` for recharts v3 peer dep. `tsc --noEmit` clean, `next build` 19 routes (executive 134 kB first-load).
- 2026-05-15 — PHASE_4 done. Operations dashboard for COO / Store Manager. Store selector (popover combobox; locked variant for store_manager/supervisor/employee on own store; "All stores" option for leadership). 4 priority tiles (Urgent Discounts / Pending Transfers / Stock Checks / Shelf Actions) with count, total, progress bar, CTA, deep link to filtered destinations. Value at Risk Today wide card: big tone-colored figure, saved-vs-at-risk progress bar, critical/high mini-stats, live pulse. Top 10 Risky Products Today compact table (sticky, color-coded days-to-expiry chip, action badge, predicted loss, store column hidden when scoped to one store, row → /products/[id]). Task Status Lanes (Pending / In Progress / Completed Today) each with count badge + 3 latest titles + Open link. Overdue Tasks Alert banner: appears only when overdues exist, lists top 3, Handle now CTA. Actionable Now: top 5 critical/high pending recommendations sorted by priority then net saved, inline Approve (AlertDialog with recommendation summary + recovered/cost/net-saved metrics), wires through actions-store + audit-log, sonner toast with "View tasks". Mini Heat-Map: stores × top-5 categories from current scope, 6-step intensity (emerald→amber→orange→rose), each cell shows count + ₼, click → /products?store=X&category=Y, legend. Live-feel: 30s setInterval ticks ±1 jitter on tile counts; LivePulse component shared. useSearchParams wrapped in Suspense to satisfy static prerender. `tsc --noEmit` clean, `next build` 19 routes (operations 14.1 kB / 161 kB first-load).
- 2026-05-15 — PHASE_5 done. /products power-user dashboard. URL-synced filter state via custom `useUrlFilters` hook: search, risk-level chips, store/category/supplier/action/status multi-select popovers (`MultiSelectPopover` shared), expiry date range, risk-score and confidence sliders, active filter count + Clear all, Saved Views popover (4 built-in: All Critical, Expiring Today, Dairy, My Store + user-saved). Sticky toolbar at top with backdrop blur. Three view modes via ToggleGroup: TABLE (TanStack react-table — checkbox select, sortable columns, sticky header, color-coded days-to-expiry chip, risk-score mini-bar, ActionBadge, ConfidenceBadge, StatusBadge, kebab row actions: View details / Approve / Reject / Create task / Audit log, page-size selector 25/50/100/200, pagination, row click → right Sheet preview drawer with predictions + AI recommendation card + open-details / approve / reject CTAs), GRID (ProductCard grid 1/2/3/4 cols responsive — gradient thumb, risk overlay, 3-stat row stock/expiry/risk, action badge, net-saved chip, View + approve/reject quick icons), HEATMAP (stores × categories from current filtered scope, 6-step intensity, click cell applies filter + switches to TABLE view). Inline Approve: AlertDialog with recommendation card + tasks-to-create list + optional approval note → actions-store.approve + audit-log + sonner with "View tasks". Inline Reject: right Sheet with 5 reason codes (Already sold / Discount too aggressive / Transfer not feasible / Will manage manually / Other) + optional note → actions-store.reject. Bulk action: floating selection bar (count + total net saved + Approve all / Reject all / Clear), Bulk modal lists selected with cumulative metrics + animated 800ms progress + outcome toast. CSV export streams filtered rows to a Blob and downloads. Store-bound roles auto-scope to own store. `tsc --noEmit` clean, `next build` 19 routes (products 36.7 kB / 194 kB first-load).
- 2026-05-15 — PHASE_7 done. Interactive What-If Simulator replaces the static scenario placeholder. New `WhatIfSimulator` component (single source) reused by `/products/[id]` (embedded, ties Approve → ApproveDialog) and `/whatif-lab` (standalone, sandbox mode). Section A: 5 scenario cards (no_action / discount / transfer / bundle / shelf_visibility) + a big combined card, each with icon, description, live recomputed Sold/Recovered/Cost (hover breakdown) + huge tone-colored Net Saved + confidence progress + per-card params (discount slider 5-50%, transfer target Select + qty slider + live cost preview + ≤1-day-expiry warning, bundle companion Select + 5-25% slider). HoverCard formula popovers per scenario. "Recommended" badge transitions to whichever scenario has the highest net_saved across recomputes; not-viable cards (transfer cost > recovery, expiry ≤ 1) get a slate overlay with reason. Margin breach inline banner when post-discount margin < min. Section B: comparison BarChart (best bar emerald, non-viable greyed). Section C: detail panel for selected scenario with 3 mini cards (Revenue / Cost breakdown / Margin & confidence) + waterfall (baseline → +recovered → −costs → final) + narrative paragraph. Section D: sticky bottom action bar with selected summary + Reset + "Approve this scenario" (gated when no recommendation). Live-data hydration in `details-data.ts` computes candidateTargetStores from cross-store sales (last 7 days, fallback to 1.5× local velocity) and candidateCompanions from same-category active products. Snapshot persistence via new `whatif-store` (zustand persist, 20 cap, label/baseline/params/selected). Standalone `/whatif-lab`: PageHeader + sandbox banner; product Combobox (cmdk Command, searches name/SKU/barcode) + store Combobox; "Override baseline" Switch reveals manual numeric inputs (stock / velocity / days / cost / sale / min margin / confidence) with "Reset to live"; live BaselineSummary stat strip; sidebar with Saved Snapshots ScrollArea (load restores baseline + selects override + jumps product/store, remove individual or Clear all). Sandbox Apply emits a toast only — never writes to operations. `tsc --noEmit` clean, `next build` 19 routes (whatif-lab 7.41 kB / 295 kB; products/[id] 33.8 kB / 337 kB).
- 2026-05-15 — PHASE_8 done. /recommendations triage inbox. Header right slot: acceptance-rate pill (tone-coloured, hides until at least one decided) + Export CSV + Bulk approve / Bulk reject (disabled until selection). 6 tabs (Pending default, Approved, Rejected, Completed, Expired, All) with live count badges that react to actions-store decisions (decision overrides JSON status via `effectiveStatus`). Sticky toolbar: Store/Category/Action/Priority MultiSelectPopover, Date range, Confidence slider, Net Saved slider (₼-formatted, signed compact), "Requires my approval" toggle (only shown for approver roles, filters by `requires_approval_by_role === currentUser.role`), Sort popover (Net Saved desc default, Priority+created, Confidence, Days-to-expiry). Active-filter footer with Clear all. Card feed: framer-motion `AnimatePresence` mode="popLayout" + per-card `motion.div` with layout + fade/scale on exit so cards animate out of Pending and into Approved when decided. Each `RecommendationCard`: Row 1 identity (checkbox for selectable pending only, gradient thumb, product name link → /products/[id], SKU mono, category + store badges, created_at relative, PriorityBadge) · Row 2 ActionBadge + StatusBadge + bold Az text + italic Az reason · Row 3 Financial: Recovered / Cost mini-tiles, big tone-coloured Net Saved tile (emerald positive, rose negative), Confidence chip with mini progress, Approver role chip · Row 4 collapsible "Show N alternative scenarios" → `ScenariosMiniBar` (top 4 by net_saved, emerald/rose bars normalised to max abs, dot marks the recommended one, formatted compact ₼) + "Open in What-If Lab" → /products/[id]#whatif · Row 5 actions (Approve / Reject for pending, decided-banner for approved/rejected, View details, Open in What-If). Completed tab adds `OutcomeBlock` per card: deterministic seeded "real recovered" value vs expected, ✅ Successful / ⚠️ Partial / ❌ Failed tier, Az narrative + "Improves future AI confidence" caption. Selection state syncs to URL-less zustand decisions: bulk-select limited to pending-effective rows, auto-drops IDs that leave the selectable set on tab change. Sticky bottom selection bar (count + cumulative net saved + Approve all / Reject all / Clear) reused from products pattern. Bulk modal reuses existing `BulkActionModal` (passing visible row.row through), approve/reject reuse `ApproveDialog` and `RejectSheet`. Store-bound roles auto-scope to own store. CSV export streams the current-tab filtered set (recommendation_id, product, store, action, recommendation_text, recovered/cost/net/confidence/priority/status/approver/created_at). Empty states per tab. `tsc --noEmit` clean, `next build` 19 routes (recommendations 54.1 kB / 223 kB first-load).
- 2026-05-15 — PHASE_9 done. Tasks for manager + employee. New `src/components/tasks/*` package: shared `useTasksData` hook (load tasks/products/stores/users/recommendations + decision-derived generated tasks + per-task overrides + 60s expiry sweep), shared TaskRow + ManagerFilters/Tab/View types, scopeTasksByRole (employee → own, supervisor/store_manager → store, logistics → transfer tasks + own store, others → all), generatedTasksFor (turns approved recommendation decisions into deterministic `gen-{rec_id}-1` tasks visible everywhere), deriveLocation (seeded "Süd reyonu, rəf 3"-style chip), urgencyTone + priorityStripColor helpers. Extended actions-store with task lifecycle: `taskOverrides: Record<id, TaskOverride>` (status/priority/deadline/assignee/completed_at/completion_note/proof_image_url), `taskComments: Record<id, TaskComment[]>`, `updateTaskStatus(id, status, {note,proofUrl,userId})` auto-stamps completed_at/by, `setTaskOverride`, `addTaskComment`. Manager /tasks: PageHeader with View toggle (Table/Calendar/Workload), sticky ManagerToolbar (search + Store/Assignee/Priority/Type/Status MultiSelectPopover + date range + Clear all + visible/total ratio), 5 tabs with live counts (Pending/In Progress/Completed Today/Expired/All), ManagerTable (sticky header, checkbox select, columns title/product/store/assignee avatar/type/priority/deadline countdown (overdue=rose, <1h=rose, <4h=amber)/status badge/source badge/kebab actions: view/mark progress/mark completed/reassign/extend deadline/cancel — row click opens drawer, links honor stopPropagation), ManagerCalendar (date-fns month grid Mon-start, deadlines plotted with priority-coloured chips, today highlight, +N overflow, nav with Today shortcut), ManagerWorkload (per assignee row with avatar+name+department, today pending/in_progress/completed stacked bar, this-week pending count, "Bottleneck" badge when >5 high-priority pending, 3 latest task chips). Sticky bottom Bulk Bar: count + critical/high mini-stats + Priority Select+Apply + Reassign Select+Apply + Cancel all + clear. Reassign / Extend deadline open shadcn Dialogs (datetime-local input for extend). Task Detail Drawer reused: right Sheet w/ title+description, status+priority chips, 4-info grid (type/deadline/store/assignee), product mini card → /products/[id] with derived location, source recommendation link, vertical status timeline (pending→assigned→in_progress→completed) with completed_at timestamp + expired/cancelled extra steps, comments thread (deterministic seeded 0/1/2 seed comments per task + live comments from store) with avatar+name+relative time, draft + send, Complete section (textarea note + mock photo upload button toast), bottom action bar (Mark in progress + Mark completed). Every transition pushes to actions-store + audit log + sonner toast. Employee /my-tasks: centered max-w-md gradient header (avatar + "Salam, {firstName} 👋" + N tasks today + 64px ProgressRing showing pct + completed/total with emerald fill on 100%), Today only Switch + active/expired count chip. Sorted task feed via framer-motion AnimatePresence popLayout: EmployeeTaskCard (priority strip 1.5px on left edge, 16px product thumb, big title, location chip, type icon+label chip, urgency pill (>4h muted, 1-4h amber, <1h rose pulse, overdue rose), full-width Tamamla primary + Detallar outline + kebab Skip/Report). Confetti empty state via framer-motion when no active+expired tasks (28 colored squares falling, "Bu gün üçün tapşırıq qalmadı 🎉"). Collapsible "Bu gün tamamlananlar" feed at bottom with faded cards + completion note. /my-tasks/[id] full-screen: back link, hero gradient product card + priority/status chips + title + product link + description + 2x2 stat grid (type/deadline/store/derived location), Mərhələlər checklist (7 per-task-type seeded checklists in Az, checkable, line-through on done), Foto və qeyd section (h-12 photo button with Loader→Camera→ImageIcon states + textarea note), sticky bottom Tamamla pill (h-12 with confirm AlertDialog → updates store + audit + toast + router.push back). `tsc --noEmit` clean, `next build` 19 routes (/tasks 20.1 kB / 209 kB; /my-tasks 5.6 kB / 210 kB; /my-tasks/[id] 11.1 kB / 155 kB).
- 2026-05-15 — PHASE_10 done. /transfers + /discounts approval queues. actions-store extended with `transferOverrides` (status, completed_at, approvals[], rejection_reason) + `discountOverrides` (status, discount_pct, time window, margin, override_below_margin) + helpers (setTransferStatus, approveTransferStep, setDiscountStatus, setDiscountOverride). /transfers: shared `useTransfersData` hook (joins transfers↔products↔stores↔recommendations + override merge), NetworkMap (SVG Baku/Sumqayıt with grid pattern + Caspian Sea hint + 8 store pins colored by usage source=sky/target=emerald/both=purple/idle=slate, hover HoverCard with name+code+address+from/to counts, click filters list, animated dashed lines with arrowheads for active transfers via @keyframes transfer-dash stroke-dashoffset), 5 tabs (Suggested/In Flight/Completed/Cancelled/All) with live count badges, sticky TransfersToolbar (search + From/To/Priority/Status MultiSelectPopover + date range + Clear all + visible/total), framer-motion AnimatePresence popLayout card feed, TransferCard (from-store ArrowRight to-store + status badge + relative time + qty + 3-stat row (cost/expected/net) with emerald/rose tone + reason chip + Approve/Reject/Details), TransferDetailDrawer (right Sheet w/ source+target store cards (address, haversine distance km, ETA ~ km/35 h), product mini card → /products/[id], source-risk vs target-demand mini-bars, cost breakdown (transport 70% / handling 25% / operational 5% / total / expected revenue / net saved), 5-step pipeline stepper (Prepare→Pickup→In transit→Receive→Confirm) with Progress + per-step circle (done emerald check / active sky / pending slate) + Advance-to-next button, multi-step approval (source_manager / target_manager / logistics) with auto-mark on approve, audit footer (created/completed/source rec)), scopeTransfersByRole (employee→none, store_manager/supervisor→from_store or to_store equals their store, others→all), bulk approve floating Selection Bar for logistics_manager/coo/ceo (count + net saved + Approve all/Cancel all/Clear), every transition pushes setTransferStatus + appendAudit + sonner toast. /discounts: shared `useDiscountsData` hook (joins discounts↔products↔stores↔recommendations + override merge + marginBreached + isLiveNow computed against MOCK_DATE 2026-05-15T12:00), 4 tabs (Suggested/Active/Completed/Rejected) with live count badges, StatsStrip (Active today / Revenue at discount today / Avg discount % / Effectiveness score with emerald/rose tone), ActiveLiveSection (only renders if liveRows>0, grid of mini cards with discount % chip, Progress bar of seeded units/dailyTarget, sold count + recovered AZN tabular, ETA hours, setInterval(2.4s) mock increments 0-2/tick), sticky DiscountsToolbar (search + Store/Priority/Status MultiSelectPopover + "Margin breach only" toggle + Clear all + visible/total), framer-motion AnimatePresence popLayout card feed, DiscountCard (LIVE pulse pill top-right when active, gradient amber thumb, product link → /products/[id], priority + status badge, pricing row with strike-through original + bold discounted + −% chip + margin chip (rose if breached) showing actual / min%, 3-stat row uplift+%/start/end, sold-units mini-bar when live, reason text, Approve/Reject/Adjust/Details actions for pending), DiscountAdjustModal (Dialog w/ discount slider 5-50% step 5, live final-price + margin recalc against product.minimum_margin_pct, margin breach inline rose banner, datetime-local Start/End inputs, Effective immediately Switch (disables start), Override margin breach Switch (gates Save when breached), → setDiscountOverride + audit + toast), DiscountDetailDrawer (right Sheet w/ product card + pricing breakdown + margin row + uplift + time window + source recommendation link + Az reason text), MarginAlarm (fixed bottom-right rose card, renders when liveRows.marginBreached.length > 0, lists top 3 breached + Review CTA that switches to Active tab + marginBreachedOnly=true + Dismiss/Close), scopeDiscountsByRole (store_manager/supervisor→own store only, employee/logistics→none, others→all). Both pages reuse PageHeader, MultiSelectPopover, PriorityBadge, Tabs/Badge/Sheet/Dialog/Slider/Switch/Progress/Sonner; route nav already wired via Truck/Percent icons in constants.ts NAV_GROUPS. `tsc --noEmit` clean, `next build` 19 routes (/transfers 20.9 kB / 223 kB; /discounts 13.5 kB / 223 kB).
- 2026-05-15 — PHASE_11 done. /analytics six-tab deep dive. Header right slot: Compare-to-previous Switch + Export PDF mock (sonner toast). Segmented Tabs (Loss & Saved / Store Performance / Category Insights / Supplier Performance / AI Performance / Sustainability) reuse shadcn Tabs, flex-wrap on small screens. New `src/lib/analytics-utils.ts` shared helpers (resolveRange ↔ filters-store dateRangeKey + customRange — produces from/to/prevFrom/prevTo/days for compare overlays; inRange; globalSnapshots/storeSnapshots/categorySnapshots; sumPeriod aggregates KpiSnapshots; deltaPct/pctSignDirection; weekKey; downloadCsv blob helper; ACTION_LABELS map; CO2_PER_KG_FOOD=2.5 / KG_PER_PARCEL=5 / KG_CO2_PER_TREE_YEAR=21 constants). New `src/components/analytics/*` package with shell + 6 tab components. Tab 1 Loss & Saved: 4-card KPI strip (Total Potential / Total Actual / Total Net Saved / Saved-to-Loss ratio) with tone-coloured change badges respecting good-direction; stacked AreaChart over time with gradient fills for potential/actual/recovered + optional dashed prev-recovered overlay driven by compare toggle; Cost donut (Discount/Transfer/Operational derived from snapshots + transfers + discounts) with legend list; Net Saved waterfall by action type — clamped to top 5 by abs(net), tones for gain (emerald) / loss (rose) / base / end; ROI horizontal BarChart per action_type (cell-tinted by sign); AI Narrative Insights card with template Az lines comparing dir of net saved + top action + saved/loss ratio + actionable note when actual > netSaved; CSV mini-action streams daily stacked data. Tab 2 Store Performance: Store × Day heatmap (auto-picks evenly spaced 14 buckets across range, 5-step intensity emerald→muted→rose normalised to max|value|, legend strip, tooltip + value glyph); Store Ranking Table with column sort (Net/Potential/Actual/Acceptance/Risky) + medal on #1 + acceptance pill (emerald/amber/rose) + deep link to /operations?store=…; Top-3 vs Bottom-3 horizontal BarChart (emerald top / rose bottom); Narrative card with Az lines (best/worst/avg acceptance). CSV export of full ranking. Tab 3 Category Insights: Loss-Share donut (top 8) with clickable legend + slice → highlight; Category × Week heatmap (top 8 by loss share, 3-step intensity); Top-10 SKUs table with predicted-loss tone-coloured value + product link → /products/[id] + category outline pill; Seasonality LineChart (top 4 categories × weekly recovered, distinct color palette, legend); Lightbulb recommendation card with Az insight + deep link to filtered /products. Tab 4 Supplier Performance: Supplier Table (volume ₼ from waste-records in range, damage_rate (rose if >5%), on-time (tone-tiered), expiry_issues count, risk pill 50/40/0); Quadrant Scatter (On-time × Damage) with ReferenceArea bands for "best" and "worst" quadrants, bubble sized by product count, fill by risk score; Supplier Trend LineChart fed by per-supplier weekly waste value; auto narrative for worst supplier. Tab 5 AI Performance: Funnel — 4 steps (Generated → Approved → Completed → Successful) with bar widths normalized + drop-from-prev pct; Acceptance Rate LineChart with 75% target ReferenceLine; Confidence vs Success ScatterChart (size by net saved, color by success tier); Reject Reasons donut + scrollable legend (merges JSON rejection_reason + live decisions reason_codes); Effectiveness card (Acceptance / Success / Avg confidence) with tone-coloured Progress; Improvement Suggestions auto-generated based on thresholds. Tab 6 Sustainability: hero Avoided Waste card (huge emerald 6xl kg figure + parcels/meals subtitle) + Tree Equivalent card (KG_CO2_PER_TREE_YEAR-based); 3 simple stat cards (CO₂ avoided / Donation-ready parcels / Forest equivalent); Monthly Trend AreaChart kg saved per month with emerald gradient; ESG hero CTA banner opens Dialog with mock PDF preview (SVG with green header + 5 bullet metrics + UN SDG alignment footer + Download/Close buttons that sonner-toast a "downloading" mock). All tabs respect filters-store date range via shared resolveRange; each chart uses Recharts ResponsiveContainer with consistent hsl token colours + animated Bars/Lines (600-700ms); empty states reusable Empty helper. `tsc --noEmit` clean, `next build` 19 routes (/analytics 28.2 kB / 325 kB first-load).
- 2026-05-15 — PHASE_12 done. /data-quality integrity console for CIO/Admin. New `src/components/data-quality/*` package + shared `useDataQualityData` hook (joins data-quality-issues ↔ products ↔ stores + applies live override merge from actions-store) + IssueRow type. actions-store extended with `issueOverrides: Record<id, IssueOverride>` (status/resolved_at/resolution_note/resolved_by_user_id) + helpers `setIssueOverride` and `setIssueStatus(id, status, {note, userId})` that auto-stamps resolved_at and reset on reopen; full reset includes issueOverrides. PageHeader actions: Run network-wide stock check (AlertDialog confirming N tasks for active stores + audit) + Export CSV. Top row 3-col grid: HealthScore radial (Recharts RadialBarChart 0..100 from avg inventory snapshot confidence × 100, tone tiered Excellent/Healthy/Needs attention/Critical with center label + descriptor) + KpiStrip (Active issues / High severity / Resolved this week / Avg time to resolve in hours computed from created_at vs effectiveResolvedAt) + IssueTypesDonut (active-only Pie with 6-color palette per issue_type, scrollable legend with All/per-type buttons that toggle the global type filter, count badges, center N active). IntegrationStatusPanel (POS/ERP/Supplier Portal/Inventory Scanner): 4 cards with icon, status pill (healthy/degraded/syncing dot), last-sync relative time, description, per-card Refresh (1.2s mock then healthy + toast), View logs (Dialog with ScrollArea of mock log lines per integration), Refresh-all (3s Progress + setInterval ticker + bulk toast). TrendsCharts 3-col grid: confidence LineChart over last 30 days from kpi_snapshots.avg_data_confidence (global rows, *100, sky stroke, % axis), Created vs Resolved BarChart over last 14 days (orange/emerald), Top-5 stores by issue count horizontal mini-bars (rose, normalized). IssuesToolbar sticky bar with Search + Type Select + Severity Select + Status Select + Clear + visible/total ratio. IssuesTable: checkbox select-all/per-row, columns Type (icon + label), Severity badge, Product (link → /products/[id]), Store (link → /operations?store=…), Description (line-clamp-2), Created relative, Status badge, kebab dropdown (View details / Mark investigating / Resolve / Ignore — disabled when already at status; Resolve opens drawer instead of quick-applying to enforce note-required rule). Row click opens IssueDetailDrawer (right Sheet): severity + status + relative time badges header, issue type title, description, Affected entities 2-card grid (product → product page link with SKU, store → operations link with code, batch row when batch_id), Suggested fix per-type checklist (6 hard-coded Az checklists, line-through on tick), "Create stock check task" full-width button (audit + toast, disabled when no product/store), Audit section (created + resolved timestamps + resolution note), Resolution note Textarea required for Mark resolved (rejects empty with toast.error), Investigating + Ignore secondary buttons. BulkActionsBar floating pill (selection count + high-severity sub-count + Create stock checks + Resolve + Ignore + Clear). Every status transition pushes setIssueStatus + appendAudit (entity_type "data_issue", action codes resolve_data_issue/ignore_data_issue/investigate_data_issue/create_stock_check_task/network_wide_stock_check) + sonner toast. Export CSV streams filtered rows (id/type/severity/product/store/description/status/created_at) via downloadCsv helper. Constants already include DATA_QUALITY_ISSUE_TYPE_LABELS/SEVERITY_LABELS/STATUS_LABELS reused throughout. `tsc --noEmit` clean, `next build` 19 routes (/data-quality 20.8 kB / 331 kB first-load).
- 2026-05-15 — PHASE_6 done. /products/[id] demo centerpiece. Client page loads product + store + category + supplier + latest risk_prediction + recommendation + scenarios + last-30-day sales + active inventory_batches + inventory_snapshots + 90-day waste + product/recommendation audit_logs + top-6 same-category related products via shared `loadProductDetailsBundle`. Sticky header: breadcrumb + thumbnail + product name + SKU/barcode (mono) + Risk score chip with RiskBadge + Audit Log button (opens drawer) + Share icon (clipboard copy + toast). 3-column lg:grid-cols-12: Col-4 product context stack (Product Info with category/supplier deep links + StorageBadge, Branch & Stock with current/available/reserved + ConfidenceBadge + last-sync, Financials with cost/sale/margin/min-margin + predicted-loss red panel, Dates with received/expiry color-coded/days-to-expiry chip/expected-sellout, Active Batches Accordion). Col-5 trends: RadialBar risk score with level color + 5 weighted factor bars + main_reason narrative; Sales Trend AreaChart 30d with rolling 7d dashed line + today ReferenceLine + Qty/Revenue toggle + 7d/30d/trend% mini-stats; Inventory Trend LineChart reconstructed back from latest snapshot via sales/replenishments + ReferenceDots on receipts + today/expiry ReferenceLines; Expiry Timeline (custom horizontal gradient bar) with Today/Sellout/Expiry markers + stacked expected-sold vs at-risk bar; Data Confidence panel (big score + 5-item checklist + "Create stock check task" CTA). Col-3 sticky AI Recommendation panel: glowing brand-bordered card with ActionBadge/ConfidenceBadge/StatusBadge/PriorityBadge, bold text + italic reason, confidence progress, 3 metric tiles (Recovered/Cost/Net Saved big), approval-role badge, Approve / Reject / Compare alternatives buttons; on decision panel flips to approved/rejected confirmation banner with "View tasks" link; collapsible Manual Action card (action-type + discount % combobox + Create task). Approve flow reuses ApproveDialog → actions-store.approve + audit-log + sonner with "View tasks"; Reject flow reuses RejectSheet with reason multi-select. What-If section (id=whatif, scroll-mt-24) renders 5 static scenario cards from `recommendation-scenarios.json` with per-type icon/description/metrics/Net-Saved tone + "Recommended" badge on `is_recommended` + "Interactive simulator unlocks in PHASE 7" lock banner. Audit Trail vertical timeline merges base audit_logs + actions-store.auditEntries scoped to this recommendation, color-coded dots + avatar + relative time. Related Products horizontal ScrollArea: 6 same-category cards with thumb/risk-badge/stock/expiry/loss stats → /products/[id]. Audit Log right Sheet: free-text action filter + expandable diff (old/new JSON). Mobile bottom action bar (Approve/Reject/Details) when recommendation present. Empty/error: product-not-found state with Back to Products. Compare-alternatives button smooth-scrolls to #whatif. `tsc --noEmit` clean, `next build` 19 routes (products/[id] 37.3 kB / 318 kB first-load).
